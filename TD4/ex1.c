#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/*
void caract()
{
    char *chaine = malloc(1000 * sizeof(char));
    int longueur = 0;
    int majuscules = 0;
    int minuscules = 0;
    int espaces = 0;
    int autres = 0;

    printf("Entrez votre chaine de caractere : ");
    fgets(chaine, 1000, stdin);

    // On parcours la chaîne caractère par caractère
    while (chaine[longueur] != '\0') {
        char caractere = chaine[longueur];
        longueur++;     //On augmente le compteur de la longueur

        if (isupper(caractere)) {   //On regarde si c'est une majuscule
            majuscules++;
        } else if (islower(caractere)) {    //On regarde si c'est une minuscule
            minuscules++;
        } else if (isspace(caractere)) {    //On regarde si c'est un espace
            espaces++;
        } else {
            autres++;
        }
    }

    printf("Longueur totale de la chaine : %d\n", longueur);
    printf("Nombre de caracteres majuscules : %d\n", majuscules);
    printf("Nombre de caracteres minuscules : %d\n", minuscules);
    printf("Nombre d'espaces : %d\n", espaces);
    printf("Nombre d'autres caracteres : %d\n", autres);
}

int main()
{
    caract();
}
*/
