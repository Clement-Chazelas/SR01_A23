#include <stdio.h>
#include <stdlib.h>
#define TAILLE  10
/*
int main()
{
    int espace, etoiles;

    for(int i = 1; i <= TAILLE; i++) {
        for(espace = 1; espace <= TAILLE - i; espace++) {   //On commence par afficher les espaces avant les étoiles
            printf(" ");
        }
        for(etoiles = 1; etoiles <= 2 * i - 1; etoiles++) {     //On affiche le nombre d'étoiles corespondant par ligne (Il s'agit d'un multiple de 2 - 1)
            printf("*");
        }
        printf("\n");   //On passe à la ligne suivante
    }

    for(int i=0;i<3; i++) {     //On affiche ensuite le tronc
        for(int j=0;j<(TAILLE/2) +2; j++) {
            printf(" ");    //On se place tout d'abord au centre
        }
        printf("||||\n");   //On affiche le tronc
    }
}
*/
