#include <stdio.h>
#include <stdlib.h>

/*
int main()
{
    char  *jour[7]={"lundi", "mardi", "mercredi", "jeudi", "vendredi", "samedi", "dimanche"};
    int prem_jour;
    int date;
    int j = 0;
    printf("Entrez l'indice du premier jour du mois : ");
    scanf("%d", &prem_jour);
    printf("Entrez la date : ");
    scanf("%d", &date);
    char *jour_fin;
    for(int i=0;i<date;i++){
        if(i%6==0){
            j = 1;
        }
        else {
            jour_fin = *(jour+j);
            j++;
        }
    }
    printf("Le %d est un %s", date, jour_fin);
}
*/
