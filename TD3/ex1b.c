#include <stdio.h>
#include <stdlib.h>
#define MAX 10

/*
int main()
{
    int tab1[MAX] = {6, 4, 5, 10, 8,};
    int tab2[MAX] = {7, 8,};
    int tab3[MAX];

    int *p1 = tab1;
    int *p2 = tab2;
    int *p3 = tab3;

    for(int i=0;i<MAX;i++) {
        *(p3 + i) = *(p1 + i) + *(p2 + MAX - 1 - i);
    }
}
*/
